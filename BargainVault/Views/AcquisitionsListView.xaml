<Window x:Class="BargainVault.Views.Acquisitions.AcquisitionsListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Acquisitions List"
        Height="550"
        Width="900"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 🔍 Search Box -->
        <StackPanel Orientation="Horizontal"
                    Margin="0,0,0,8">
            <TextBlock Text="🔍 Search:"
               VerticalAlignment="Center"
               Margin="0,0,6,0"/>

            <TextBox Width="280"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Search by item title, channel, or booth"/>
        </StackPanel>

        <!-- 📋 Data Grid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding ItemsView}"
                  SelectedItem="{Binding SelectedAcquisition, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">

           

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding AcqId}" Width="60"/>
                    <DataGridTextColumn Header="Item" Binding="{Binding ItemTitle}" Width="*"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding DateAcquired}" Width="120"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalSettlement}" Width="120"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding StatusName}" Width="140"/>
                    <DataGridCheckBoxColumn Header="Personal" Binding="{Binding Personal}" Width="80"/>
                    <DataGridCheckBoxColumn Header="Business" Binding="{Binding BusinessExpense}" Width="90"/>
                </DataGrid.Columns>

           
        </DataGrid>

        <!-- 🔘 Action Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">

            <Button Content="✏️ Edit"
                    Width="90"
                    Margin="4"
                    Click="EditSelected_Click"
                    IsEnabled="{Binding HasSelection}"/>

            <Button Content="🗑 Delete"
                    Width="90"
                    Margin="4"
                    Click="DeleteSelected_Click"
                    IsEnabled="{Binding HasSelection}"/>

            <Button Content="Close"
                    Width="90"
                    Margin="4"
                    Click="Close"/>
        </StackPanel>

    </Grid>
</Window>
