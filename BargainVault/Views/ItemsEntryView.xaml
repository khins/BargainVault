<Window x:Class="BargainVault.Views.Items.ItemsEntryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Item Entry"
        Width="500"
        Height="350"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>

        <Style x:Key="MainMenuButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#707070"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="Black"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                       x:Name="border"
                       Background="{TemplateBinding Background}"
                       BorderBrush="{TemplateBinding BorderBrush}"
                       BorderThickness="{TemplateBinding BorderThickness}"
                       CornerRadius="6">

                            <ContentPresenter
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           RecognizesAccessKey="True"/>
                        </Border>

                        <!-- Visual states -->
                        <ControlTemplate.Triggers>

                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border"
                                   Property="Background"
                                   Value="#D6D6D6"/>
                            </Trigger>

                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border"
                                   Property="Background"
                                   Value="#C8C8C8"/>
                            </Trigger>

                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border"
                                   Property="Background"
                                   Value="#EEEEEE"/>
                                <Setter Property="Foreground"
                                   Value="#888888"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Lot Number -->
        <TextBlock Text="Lot Number" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding LotNumber}" Grid.Row="0" Grid.Column="1"/>

        <!-- Title -->
        <TextBlock Text="Title" Grid.Row="1" Grid.Column="0" Margin="0,10,0,0"/>
        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="1" Grid.Column="1" Margin="0,10,0,0"/>

        <!-- Description -->
        <TextBlock Text="Description" Grid.Row="2" Grid.Column="0" Margin="0,10,0,0"/>
        <TextBox Text="{Binding Description}"
                 Grid.Row="2" Grid.Column="1"
                 Height="80"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 Margin="0,10,0,0"/>

        <!-- Created At -->
        <TextBlock Text="Created At" Grid.Row="3" Grid.Column="0" Margin="0,10,0,0"/>
        <TextBox Text="{Binding CreatedAt, Mode=OneWay, StringFormat=\{0:G\}}"
                 Grid.Row="3" Grid.Column="1"
                 IsReadOnly="True"
                 Margin="0,10,0,0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button Content="Save Entry"
Style="{StaticResource MainMenuButtonStyle}"
                    Command="{Binding SaveCommand}" />

            <Button Content="New Entry"
Style="{StaticResource MainMenuButtonStyle}"
                    Command="{Binding NewEntryCommand}"/>

            <Button Content="Close"
Style="{StaticResource MainMenuButtonStyle}"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
