<Window x:Class="BargainVault.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BargainVault"
        Width="420"
        Height="575"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>

        <Style x:Key="MainMenuButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#707070"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="Black"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6">

                            <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            RecognizesAccessKey="True"/>
                        </Border>

                        <!-- Visual states -->
                        <ControlTemplate.Triggers>

                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border"
                                    Property="Background"
                                    Value="#D6D6D6"/>
                            </Trigger>

                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border"
                                    Property="Background"
                                    Value="#C8C8C8"/>
                            </Trigger>

                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border"
                                    Property="Background"
                                    Value="#EEEEEE"/>
                                <Setter Property="Foreground"
                                    Value="#888888"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Border Padding="20"
                CornerRadius="6"
                BorderThickness="6"
                BorderBrush="#DDD"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel Width="240"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                <!-- Logo -->
                <Image Source="pack://application:,,,/Assets/BargainVaultLogo.png"
                       Height="90"
                       Stretch="Uniform"
                       Margin="0,0,0,12"/>

                <!-- App Title -->
                <TextBlock Text="Bargain Vault"
                           FontSize="20"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,12"/>

                <Button Content="New Item Entry"
        Style="{StaticResource MainMenuButtonStyle}"
        Click="OpenItemsEntry_Click"/>

                <Button Content="View / Edit Items"
        Style="{StaticResource MainMenuButtonStyle}"
        Click="OpenItemsList_Click"/>

                <Button Content="New Acquisition"
        Style="{StaticResource MainMenuButtonStyle}"
        Click="OpenAcquisitionEntry_Click"/>

                <Button Content="Acquisitions List"
        Style="{StaticResource MainMenuButtonStyle}"
        Click="OpenAcquisitionsList_Click"/>

                <Button Content="💰 New Sale"
        Style="{StaticResource MainMenuButtonStyle}"
        Click="OpenSalesEntry_Click"/>

                <Button Content="📊 Sales Dashboard"
        Style="{StaticResource MainMenuButtonStyle}"
        Click="OpenSalesDashboard_Click"/>


                <Button Content="Exit"
        Style="{StaticResource MainMenuButtonStyle}"
        Click="Exit_Click"/>



            </StackPanel>
        </Border>
    </Grid>
</Window>
